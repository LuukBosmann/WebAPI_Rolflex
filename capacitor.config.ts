import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RLS voorraad app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
